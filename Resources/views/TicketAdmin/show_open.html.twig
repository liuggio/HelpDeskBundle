{% extends 'LiuggioHelpDeskTicketSystemBundle:TicketAdmin:show.html.twig' %}

{% block comment %}
<ul class="record_actions">

    <li>
        <form action="{{ path('comment_create_admin') }}" method="post" {{ form_enctype(comment_create_admin) }}>
            {{ form_widget(comment_create_admin) }}
            <p>
                <button type="submit">{{ "replyComment_button"|trans }}</button>
            </p>
        </form>
    </li>
</ul>
{% endblock comment %}